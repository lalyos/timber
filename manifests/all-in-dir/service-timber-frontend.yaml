apiVersion: v1
kind: Service
metadata:
  labels:
    app: timber-frontend
  name: timber-frontend
spec:
  type: NodePort
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: timber-frontend
